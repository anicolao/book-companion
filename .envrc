use flake

# Ensure Ollama llama2 model is available
# Only pull if not already present to avoid unnecessary downloads
if ! ollama list | grep -q llama2; then
  echo "Downloading Ollama llama2 model..."
  ollama pull llama2
else
  echo "Ollama llama2 model already available"
fi
